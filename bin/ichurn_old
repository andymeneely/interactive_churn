#!/usr/bin/ruby

require_relative "../lib/interactive_churn.rb"
# The git revision arg, i.e. "git log #{rev}", or e.g. git log abc456..def123

gitlogarg = ARGV.empty? ? '' : ARGV[0]

InteractiveChurn::get_data(gitlogarg)
